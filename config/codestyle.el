;;; codestyle.el - indentation and other code styles all in one places
;; thanks bodil

;; Tabs are the devil.
(set-default 'indent-tabs-mode nil)

;; Default indentation
(setq-default tab-width 2)

;; Come on... really?
(setq sentence-end-double-space nil)
